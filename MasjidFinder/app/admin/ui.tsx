import { AlertCircle, CheckCircle2, Clock3, XCircle } from "lucide-react";

export function AdminHeader({ eyebrow, title, description, action }: { eyebrow?: string; title: string; description?: string; action?: React.ReactNode }) { return <header className="mb-7 flex flex-col gap-4 border-b border-black/5 pb-6 sm:flex-row sm:items-end sm:justify-between"><div><p className="mb-2 text-[10px] font-bold uppercase tracking-[0.18em] text-gold-600">{eyebrow ?? "Admin workspace"}</p><h1 className="text-2xl font-bold tracking-tight text-emerald-900">{title}</h1>{description && <p className="mt-1 max-w-2xl text-sm text-ink-600">{description}</p>}</div>{action}</header>; }
export function StatCard({ label, value, detail, tone = "default" }: { label: string; value: string | number; detail?: string; tone?: "default" | "gold" | "red" }) { return <div className={`rounded-lg border bg-white p-4 ${tone === "gold" ? "border-gold-500/30" : tone === "red" ? "border-red-200" : "border-black/8"}`}><p className="text-xs font-semibold text-ink-600">{label}</p><p className="mt-2 text-2xl font-bold tracking-tight text-emerald-900">{value}</p>{detail && <p className="mt-1 text-xs text-ink-400">{detail}</p>}</div>; }
export function StatusBadge({ status }: { status: string | null | undefined }) { const value = status ?? "unknown"; const styles: Record<string, string> = { verified: "bg-emerald-100 text-emerald-700", active: "bg-emerald-100 text-emerald-700", open: "bg-gold-500/15 text-gold-600", pending: "bg-amber-100 text-amber-700", rejected: "bg-red-100 text-red-700", cancelled: "bg-red-100 text-red-700", banned: "bg-red-100 text-red-700", inactive: "bg-sand-100 text-ink-600" }; const Icon = value === "verified" || value === "active" ? CheckCircle2 : value === "pending" || value === "open" ? Clock3 : value === "rejected" || value === "banned" || value === "cancelled" ? XCircle : AlertCircle; return <span className={`inline-flex items-center gap-1 rounded-full px-2 py-1 text-[10px] font-bold capitalize ${styles[value] ?? "bg-sand-100 text-ink-600"}`}><Icon size={12} />{value.replaceAll("_", " ")}</span>; }
export function EmptyState({ title, description }: { title: string; description: string }) { return <div className="rounded-lg border border-dashed border-black/10 bg-white p-10 text-center"><p className="font-semibold text-emerald-900">{title}</p><p className="mt-1 text-sm text-ink-400">{description}</p></div>; }
export function ErrorState({ message }: { message: string }) { return <div className="rounded-lg border border-red-200 bg-red-50 p-4 text-sm text-red-700"><strong>Could not load this view.</strong> {message}</div>; }