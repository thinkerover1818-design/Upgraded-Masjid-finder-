import { revalidatePath } from "next/cache";
import { AdminHeader, EmptyState, ErrorState, StatusBadge } from "../ui";
import { adminDb, dateLabel, queryText, requireAdmin } from "../admin-data";
import ConfirmButton from "../ConfirmButton";

export const dynamic = "force-dynamic";
async function setMasjidStatus(formData: FormData) { "use server"; const db = adminDb(); const { data: { user } } = await db.auth.getUser(); if (!user) return; await db.from("masjids").update({ is_active: String(formData.get("action")) === "activate" }).eq("id", String(formData.get("id"))); revalidatePath("/admin/masjids"); }
export default async function MasjidsPage({ searchParams }: { searchParams: { q?: string } }) { const context = await requireAdmin(); if (!context) return <main className="p-8 text-sm text-red-700">Your account is not an admin.</main>; const q = queryText(searchParams.q); let request = context.supabase.from("masjids").select("id, account_code, masjid_name, city_id, verification_status, is_active, created_at").is("deleted_at", null).order("created_at", { ascending: false }).limit(50); if (q) request = request.or(`masjid_name.ilike.%${q}%,account_code.ilike.%${q}%`); const { data: rows, error } = await request; return <main className="mx-auto max-w-7xl p-4 sm:p-6 lg:p-8"><AdminHeader title="Masjids" description="Review and manage masjid listings. Exact addresses remain outside the admin list view." /><form className="mb-5 flex gap-2"><input name="q" defaultValue={q} placeholder="Search masjid or account code" className="min-w-0 flex-1 rounded-md border border-black/10 bg-white px-3 py-2.5 text-sm" /><button className="rounded-md bg-emerald-900 px-4 py-2.5 text-sm font-semibold text-white">Search</button></form>{error ? <ErrorState message={error.message} /> : !rows?.length ? <EmptyState title="No masjids found" description="Masjid listings will appear here when they are registered." /> : <div className="overflow-x-auto rounded-lg border border-black/8 bg-white"><table className="w-full min-w-[650px] text-left text-sm"><thead className="border-b border-black/5 bg-sand-50 text-xs text-ink-600"><tr><th className="px-4 py-3">Masjid</th><th className="px-4 py-3">Verification</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Created</th><th className="px-4 py-3">Action</th></tr></thead><tbody className="divide-y divide-black/5">{rows.map((item: any) => <tr key={item.id}><td className="px-4 py-3"><p className="font-semibold text-emerald-900">{item.masjid_name}</p><p className="text-xs text-ink-400">{item.account_code}</p></td><td className="px-4 py-3"><StatusBadge status={item.verification_status} /></td><td className="px-4 py-3"><StatusBadge status={item.is_active ? "active" : "inactive"} /></td><td className="px-4 py-3 text-xs text-ink-600">{dateLabel(item.created_at)}</td><td className="px-4 py-3"><form action={setMasjidStatus}><input type="hidden" name="id" value={item.id} /><ConfirmButton name="action" value={item.is_active ? "deactivate" : "activate"} message={item.is_active ? "Deactivate this masjid?" : "Activate this masjid?"} className="text-xs font-semibold text-emerald-700">{item.is_active ? "Deactivate" : "Activate"}</ConfirmButton></form></td></tr>)}</tbody></table></div>}</main>; }