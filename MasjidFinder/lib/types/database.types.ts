/**
 * PLACEHOLDER TYPES.
 *
 * This file is a loose stand-in so the project type-checks before you have a
 * live Supabase project to introspect. Once you've run the migrations against
 * a real project, replace this file with the real generated types:
 *
 *   npx supabase login
 *   npx supabase link --project-ref YOUR_PROJECT_REF
 *   npm run db:types
 *
 * Until then, every table/view/RPC is typed as `any` so builds succeed, but
 * you get NO compile-time safety on Supabase queries — do not ship to
 * production on placeholder types.
 */

export type Database = any;
